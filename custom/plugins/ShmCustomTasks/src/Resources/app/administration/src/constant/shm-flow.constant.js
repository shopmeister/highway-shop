export const ACTION = Object.freeze({
    CHANGE_SHIPPING: 'action.change.shipping',
    CREATE_RETURN_LABEL: 'action.create.return-label'
});

export const GROUP = 'order';

export default {
    ACTION, GROUP
};