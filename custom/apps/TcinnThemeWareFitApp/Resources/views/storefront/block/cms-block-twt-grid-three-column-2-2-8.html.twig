{# ThemeWare® CMS block "Three columns 2-2-8" #}

{% block block_twt_grid_three_column_2_2_8 %}
    {% set columns = 3 %}

    {% block block_twt_grid_three_column_2_2_8_left %}
        {% set element = block.slots.getSlot('left') %}

        {# Set image size for every viewport #}
        {# @TODO... #}

        {# Set image size for largest viewport depending on the cms block sizing mode (boxed or full-width) #}
        {# @TODO... #}

        <div class="column-1 col-12 col-md-6 col-lg-2 twt-column left first" data-cms-element-id="{{ element.id }}">
            {% block block_twt_grid_three_column_2_2_8_left_inner %}
                {% sw_include '@Storefront/storefront/element/cms-element-' ~ element.type ~ '.html.twig' ignore missing %}
            {% endblock %}
        </div>
    {% endblock %}

    {% block block_twt_grid_three_column_2_2_8_center %}
        {% set element = block.slots.getSlot('center') %}

        {# Set image size for every viewport #}
        {# @TODO... #}

        {# Set image size for largest viewport depending on the cms block sizing mode (boxed or full-width) #}
        {# @TODO... #}

        <div class="column-2 col-12 col-md-6 col-lg-2 twt-column center" data-cms-element-id="{{ element.id }}">
            {% block block_twt_grid_three_column_2_2_8_center_inner %}
                {% sw_include '@Storefront/storefront/element/cms-element-' ~ element.type ~ '.html.twig' ignore missing %}
            {% endblock %}
        </div>
    {% endblock %}

    {% block block_twt_grid_three_column_2_2_8_right %}
        {% set element = block.slots.getSlot('right') %}

        {# Set image size for every viewport #}
        {# @TODO... #}

        {# Set image size for largest viewport depending on the cms block sizing mode (boxed or full-width) #}
        {# @TODO... #}

        <div class="column-3 col-12 col-lg-8 twt-column right last" data-cms-element-id="{{ element.id }}">
            {% block block_twt_grid_three_column_2_2_8_right_inner %}
                {% sw_include '@Storefront/storefront/element/cms-element-' ~ element.type ~ '.html.twig' ignore missing %}
            {% endblock %}
        </div>
    {% endblock %}
{% endblock %}