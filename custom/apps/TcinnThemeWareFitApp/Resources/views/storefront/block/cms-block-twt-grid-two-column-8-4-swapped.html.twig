{# ThemeWare® CMS block "Two columns 8-4 (swapped)" #}

{% block block_twt_grid_two_column_8_4_swapped %}
    {% set columns = 2 %}

    {% block block_twt_grid_two_column_8_4_swapped_left %}
        {% set element = block.slots.getSlot('left') %}

        {# Set image size for every viewport #}
        {# @TODO... #}

        {# Set image size for largest viewport depending on the cms block sizing mode (boxed or full-width) #}
        {# @TODO... #}

        <div class="column-1 col-12 col-md-8 order-2 order-md-1 twt-column left first" data-cms-element-id="{{ element.id }}">
            {% block block_twt_grid_two_column_8_4_swapped_left_inner %}
                {% sw_include '@Storefront/storefront/element/cms-element-' ~ element.type ~ '.html.twig' ignore missing %}
            {% endblock %}
        </div>
    {% endblock %}

    {% block block_twt_grid_two_column_8_4_swapped_right %}
        {% set element = block.slots.getSlot('right') %}

        {# Set image size for every viewport #}
        {# @TODO... #}

        {# Set image size for largest viewport depending on the cms block sizing mode (boxed or full-width) #}
        {# @TODO... #}

        <div class="column-2 col-12 col-md-4 order-1 order-md-2 twt-column right last" data-cms-element-id="{{ element.id }}">
            {% block block_twt_grid_two_column_8_4_swapped_right_inner %}
                {% sw_include '@Storefront/storefront/element/cms-element-' ~ element.type ~ '.html.twig' ignore missing %}
            {% endblock %}
        </div>
    {% endblock %}
{% endblock %}